# test-aws-lambda

AWS Lambda の並列呼び出しの振る舞いを検証するプロジェクト。

## 目的

Lambda 関数を呼び出す際、並列呼び出しが **可能な場合** と **不可能な場合** でそれぞれどのような挙動になるかを確認する。

## アプローチ

- インフラのセットアップには `aws` CLI を使用する（Terraform は不要な複雑さを避けるため使わない）
- テストコードは **Rust** で記述する

## 注意

検証によって作成・変更した AWS リソースは、必ず元の状態に戻すこと。
